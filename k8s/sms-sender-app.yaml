apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sms-sender-worker
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/soldesk-vital/soldesk-project2'
    targetRevision: main
    path: sms_sender_worker/k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: sms-sender-worker
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true 